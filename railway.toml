[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Do not set startCommand here — it overrides the Dockerfile CMD and can break $PORT.
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
